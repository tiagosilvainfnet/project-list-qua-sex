import * as React from 'react';
import {IconButton as Icb, IconButtonProps, MD3Colors} from 'react-native-paper';

const IconButton = (props: IconButtonProps) => (
    <Icb {...props} />
);

export default IconButton;